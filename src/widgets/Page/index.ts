import { scrollReducer } from './model/slices/ScrollSlice';
import { ScrollSchema } from './model/types/scrollSchema';
import Page from './ui/Page';

export {
    Page, ScrollSchema, scrollReducer
};

